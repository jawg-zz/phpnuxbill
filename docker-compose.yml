services:
  nuxbill:
    pull_policy: always
    build: ./
    environment:
      TZ: Africa/Nairobi
    volumes:
      - site_data/uploads:/var/www/html/system/uploads
      - site_data/config.php:/var/www/html/config.php

